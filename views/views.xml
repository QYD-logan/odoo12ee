<odoo>
    <data>


        <!-- Action to open the Book list -->
        <act_window id="action_add_label"
                    name="添加标签"
                    res_model="add.label"
                    view_mode="tree,form"
        />
        <!-- Menu item to open the Book list -->
        <menuitem id="menu_add_label"
                  name="标签"
                  parent="base.menu_email"/>
    </data>
</odoo>